import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    background: {
      default: '#FFF',
    },
  },
  typography: {
    fontFamily: '"Poppins", sans-serif',
    fontSize: 14,
  },
});
