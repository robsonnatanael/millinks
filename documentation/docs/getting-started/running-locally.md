# Running locally

After installing the dependencies, you can start the development environment.

```bash
npm run dev
```

The application will be available at:

http://localhost:3000

The server will automatically reload when you change project files.
